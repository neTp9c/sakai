<?xml version="1.0" encoding="UTF-8"?>
<emailTemplates>
    <emailTemplate>
        <subject>Уведомление об отправке теста: ${siteName} - ${assessmentTitle}</subject>
        <message>Следующая попытка сдачи теста была зафиксирована:

            Название сайта         : ${siteName}
            Тест                   : ${assessmentTitle}

            Студент                : ${userName} (${userDisplayID})
            Идентификатор попытки  : ${assessmentGradingID}
            Дата попытки           : ${submissionDate}
            Номер подтверждения    : ${confirmationNumber}

            Тест продлится до      : ${assessmentDueDate}
            Идентификатор сайта    : ${siteID}

            ---
            Это уведомление было отправлено автоматически.
        </message>
        <messagehtml>&lt;pre&gt;
            Следующая попытка сдачи теста была зафиксирована:

            Название сайта         : ${siteName}
            Тест                   : ${assessmentTitle}

            Студент                : ${userName} (${userDisplayID})
            Идентификатор попытки  : ${assessmentGradingID}
            Дата попытки           : ${submissionDate}
            Номер подтверждения    : ${confirmationNumber}

            Тест продлится до      : ${assessmentDueDate}
            Идентификатор сайта    : ${siteID}

            ---
            Это уведомление было отправлено автоматически.
            &lt;/pre&gt;
        </messagehtml>
        <version>1</version>
        <owner>admin</owner>
        <key>sam.assessmentSubmitted</key>
        <locale></locale>
        <localeLangTag></localeLangTag>
    </emailTemplate>
    <emailTemplate>
        <subject>Notification for assessment submission: ${siteName} - ${assessmentTitle}</subject>
        <message>The following assessment submission was recorded by ${localSakaiName}:

            Site Title          : ${siteName}
            Assessment          : ${assessmentTitle}

            Student             : ${userName} (${userDisplayID})
            Submission ID       : ${assessmentGradingID}
            Submitted on        : ${submissionDate}
            Confirmation Number : ${confirmationNumber}

            Assessment Due Date : ${assessmentDueDate}
            Site ID             : ${siteID}

            ---
            This automatic notification message was sent by ${localSakaiName} (${localSakaiUrl})
            ${changeSettingInstructions}
        </message>
        <messagehtml>&lt;pre&gt;
            The following assessment submission was recorded by ${localSakaiName}:

            Site Title          : ${siteName}
            Assessment          : ${assessmentTitle}

            Student             : ${userName} (${userDisplayID})
            Submission ID       : ${assessmentGradingID}
            Submitted Date      : ${submissionDate}
            Confirmation Number : ${confirmationNumber}

            Assessment Due Date : ${assessmentDueDate}
            Site ID             : ${siteID}

            ---
            This automatic notification message was sent by ${localSakaiName} (${localSakaiUrl})
            ${changeSettingInstructions}
            &lt;/pre&gt;
        </messagehtml>
        <version>1</version>
        <owner>admin</owner>
        <key>sam.assessmentSubmitted</key>
        <locale>ru</locale>
        <localeLangTag>ru</localeLangTag>
    </emailTemplate>
</emailTemplates>
